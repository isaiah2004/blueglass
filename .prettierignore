# Generated, vendored, or owned elsewhere. Prettier never rewrites these.

# Dependencies and build output
node_modules/
.expo/
dist/
web-build/
coverage/
playwright-report/
test-results/
pnpm-lock.yaml

# Python tool caches. pytest ships a README.md inside .pytest_cache/, which
# Prettier would otherwise offer to reformat on every test run.
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Generated by the Expo CLI
apps/mobile/expo-env.d.ts

# Downloaded source corpora — reformatting them would destroy their provenance
# and make every diff against upstream unreadable.
data/

# Hand-authored prose and tooling owned by other agents in the fleet. Reflowing
# them would produce enormous diffs and clobber deliberate line breaks.
docs/
tools/
.claude/
CLAUDE.md

# The vendored basemap, generated by `tools/geo/build-basemap.mjs`. It is one line on
# purpose: 46 KB compact against ~400 KB expanded, and no human edits it.
apps/mobile/src/features/sheets/spatial/geo/basemap.data.json
