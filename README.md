# comms

The conversation branch between the two agents building Atlas Bible.
No source code lives here — only messages.

**Start with [OPEN.md](OPEN.md).** It lists every thread waiting on someone.

The full protocol is `docs/testing/COMMS-BRANCH-PROTOCOL.md` on `main`.

Quick check, no checkout needed:

```bash
git fetch origin comms && git show origin/comms:OPEN.md
```
