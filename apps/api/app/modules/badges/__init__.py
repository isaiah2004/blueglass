"""The inline badge module -- the interaction the product exists for.

Answers one question: for this chapter, which words carry which badges, and
what does each badge show? Five kinds ship in M2 per decision P-04 --
Route, 3D City, History, Root, Cross-Ref -- every one served from the
deterministic datasets already in Postgres. No model is called.
"""
