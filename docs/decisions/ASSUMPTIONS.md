# Provisional assumptions

Decisions taken **without** an answer from the product owner, so that work could
continue. Each one names the Question Hub id that will settle it.

**Rule:** when an answer arrives and contradicts an assumption here, the row moves to
"Resolved" with a note on what had to change. Never silently delete a row.

Regenerate the current answer state with:

```bash
node tools/question-hub/answers.mjs --all
```

## Open — acting on the recommended default

| Question | Assumption in force | What it affects | Cost if wrong |
|---|---|---|---|
| `T-10` | Expo Router, file-based, typed routes. | Navigation, deep links. | Medium. |
| `T-11` | `StyleSheet` + typed design tokens (no CSS-in-JS runtime). | Every component. | Medium. |
| `T-12` | Zustand for client state. | State layer. | Low — small surface. |
| `T-14` | TypeScript at maximum strictness. | Whole codebase. | Low — relaxing later is trivial. |
| `T-15` | pnpm workspaces (`apps/`, `packages/`). | Repo layout. | Medium — moving files later is cheap but noisy. |
| `AI-03` | Hard spend guard **and** disk response cache on every AI call. | AI layer. | None — strictly protective. Building regardless. |
| `AI-04` | "NotebookLM" in the spec means NotebookLM-*style*: our own grounded RAG with strict citations. | Studio tab architecture. | High — there is no public NotebookLM API, so this is the only buildable reading. |
| `Q-01` | Playwright against the web build for the continuous unattended walkthrough loop. | QA harness. | Low — additive. |
| `AI-02` | Per-task open-weight defaults, benchmarked not guessed: `mistral-small-3.2-24b` for extraction, `qwen3-235b-a22b-2507` for chat and editorial, `mistral-nemo` for cheap classification. See `docs/architecture/ai-model-strategy.md`. | Model registry, every AI call. | Low — the registry makes a swap a one-line change. |
| `Q-010` | OpenRouter sells **no** embedding models (verified: 0 of 388). Self-host `BAAI/bge-m3` in the existing Docker stack behind a swappable interface — $0 per embedding. | pgvector retrieval, RAG. | Low — the interface allows a paid API to be dropped in later. |
| `Q-013` | App identity written into `apps/mobile/app.json`: slug `atlas-bible`, iOS bundle id and Android package `app.atlasbible.mobile`, deep-link scheme `atlasbible`. | Store listings, deep links, every share URL, EAS build profiles. | Low **until the first store submission**, then high — a bundle id cannot be changed on a published app. Must be settled before the first EAS build. |
| `Q-018` | Eleven inline badge kinds, not ten: Route and 3D City stay separate and Lineage is kept. The PRD prose says ten but lists eleven marks; `design-language.md` §2 gives a hue to ten and omits Lineage. | `packages/shared/src/badges.ts` discriminated union; every badge sheet; the design token table, which still owes Lineage a hue. | Low — dropping a kind later is a union edit plus a data migration of `kind` strings; adding one back is worse. |
| `Q-019` | A pre-computed `passage_id`'s book segment is the **uppercased OSIS code** (`1COR_13_1_13`), not the uppercased full name. The PRD's only example, `ACTS_16_11_15`, cannot distinguish the two. Parsing accepts either; only formatting had to choose. | `packages/shared/src/enrichment/passage-id.ts`; every pre-computed record filename, CDN path, and cache key. | Medium — if the editorial pipeline picks the other form, every lookup misses and the pipeline re-runs. Settle before the first pre-computed batch is published. |
| `D-03` | Typefaces picked, all SIL OFL: **Source Serif 4** for scripture (the Flutter prototype's serif, and it holds at 19–21 pt), **Inter** for UI, **JetBrains Mono** for metadata. `apps/mobile/src/theme/typography.ts` names one registered face per family-and-weight pair, so `expo-font` must load exactly those eight names. | The whole visual feel; the font-loading agent's asset list. | Low — swapping a family is one edit to `fontFamily` in `typography.ts`; no component names a font. |
| `Q-017` | `ink.tertiary` (`#5D6A7D`) stays at the mockup value but is **not legal for 9–11 pt metadata** — it measures 3.36:1 on `bg.card` against WCAG AA's 4.5:1. Small metadata text uses `ink.secondary` (6.53:1 worst case); `ink.tertiary` is for large text, icons and rules, where it clears the 3:1 bar. Locked by `apps/mobile/src/theme/colors.contrast.test.ts`. | Every label, stat caption, verse reference, and inactive tab label. | Low — a usage rule, not a palette change. If the human instead lightens the token, one hex and a handful of asserted ratios move. |
| `Q-020` | AI spend guard hard ceiling: `ABSOLUTE_MAX_CEILING_USD` = $2.00 (no env var may exceed it), with defaults of $0.50 locally and $0.05 under CI. Meters on each response's `usage.cost`. See `packages/ai-guard/README.md`. | Every AI call in the project; how much of the budget an unattended loop can reach. | Low — raising the cap is a one-constant edit in `packages/ai-guard/src/config.ts`; the cost of being wrong low is a blocked pre-compute run, not lost work. |
| `Q-022` | A dropped chat stream keeps whatever text arrived and surfaces a Retry control. Nothing is re-sent automatically, so a flaky connection can never spend the budget twice without a tap. `/chat/stream` is stateless and cannot resume a partial completion, so "retry" always means a fresh model call. | The Ask tab and the reader chat's failure UX; how much a flaky network can cost. | Low — auto-retry is one call site in the streaming bubble's error branch, and the typed `ChatStreamError` codes already distinguish a retryable transport drop from a server refusal. |
| `Q-021` | The ten badge glyphs are **colour emoji** as a stopgap, which means `design-language.md` §5's "text and icon in the full hue" is knowingly violated on the icon — an emoji is painted by the OS and cannot be tinted. Recommendation queued: ten monochrome `react-native-svg` paths vendored in-repo. | `apps/mobile/src/components/InlineBadge.types.ts` (`badgeGlyph`); the look of every badge; the pill's width, because emoji metrics differ per platform. | Low — the glyph table is ten strings behind one function. But shipping emoji would break the "learn the badge types by colour" rule the design language rests on, so it must be settled before the reader screen is signed off. |
| `Q-024` | The reader ships **four** public-domain translations: **BSB, KJV, WEB, ASV** (124,372 verses, measured). **BSB is the default** — modern English, and the PRD's own stated launch preference; the choice of default is queued for the product owner and this is the recommendation taken meanwhile. The BSB's 2023 public-domain dedication is now **verified**, not assumed: berean.bible/terms.htm states it outright, quoted in `data/scripture/PROVENANCE.md`. `KJVPCE` is gone — the scrollmapper dataset behind it is corrupt (Joshua 15:1, Job 7:1, Hosea 8:1 and **Romans 8:1** are empty strings in every format it publishes), so the KJV now comes from eBible.org. **ESV is licensed, appears in the mockups, and must never ship.** | What the switcher offers; how archaic the default reading experience is; the API's `default_translation`, which moved from `KJVPCE` to `BSB`. | Low. Changing the default is one settings line. Adding a translation is one catalogue entry plus `pnpm db:seed`; every licence is recorded per translation in `data_sources`, so removing one is a delete, not an audit. |
| `Q-024` *(hub id — the device-id question; see the collision note below)* | The anonymous device id (`A-01`) is 32 hex characters behind an `atlas-` prefix, minted from `crypto.randomUUID` where it exists, `crypto.getRandomValues` where it does not, and **`Math.random` on a bare Hermes runtime with neither**. The weak path is reported, not hidden: `generateDeviceId` returns `quality: 'weak'`, so startup can act on it. The alternative — adding `expo-crypto` — is a dependency the fleet has been told to add only when needed, and the id is an assertion rather than a secret (the server's own `device_identity_resolver.py` says so). | Every request's `X-Atlas-Device-Id`; how guessable one reader's data scope is. | Low, and one file wide. If strong entropy is required everywhere, `apps/mobile/src/api/identity/device-id.ts` gains an `expo-crypto` import behind the same interface; already-minted ids stay valid because the server validates format, not provenance. |
| `B-04` | Migrations are **Alembic**, in `apps/api/db/versions/`, applied by the `migrate` compose service. The bespoke forward-only SQL runner at `infra/db/migrate.sh` was deleted — its own header named Alembic as the intended end state for this decision. Written as SQL inside `op.execute`, with no ORM models and no `--autogenerate`: the schema uses generated columns, GiST range indexes and pgvector opclasses SQLAlchemy cannot express. | Every schema change from here on; the one command that brings the stack up. | Low. Alembic's own migration files are plain Python calling SQL, so moving off it would mean re-running the same statements under a different runner, not rewriting them. |
| `Q-024` *(hub id; see the collision note below)* | Every interactive surface the walkthrough must reach carries a **stable `testID`** from the contract in `e2e/support/test-ids.ts` (~30 ids: shell, tabs, reader, translation switcher, reference picker, verse sheet, search, error state). The harness addresses elements by test id and never by visible copy, so a wording change cannot break a walkthrough and an icon-only control is still reachable. Accessibility roles and labels are a separate, later pass — the harness audits tap-target size and text legibility today, not the accessibility tree. | Every screen the three feature agents are building now; every assertion in `e2e/walkthrough/`. | Low. If the answer is roles-and-labels instead, the test ids become dead props and ~30 selectors change in one directory — the chapters, steps and audits are unaffected. |
| `Q-023` | The 300-line file limit (CLAUDE.md "Hard limits", rule 5.4.2) is read as applying to **source files**, not to Markdown. The five long documents written this session stay whole: `docs/architecture/hub-platform.md` (932), `docs/architecture/dataset-validation.md` (605), `tools/question-hub/README.md` (515), `docs/DEVELOPMENT.md` (451), `docs/architecture/spike-sse.md` (372) — alongside the pre-existing 760-line `flutter-port-map.md` the briefing already sanctions. Every source file in the repo is under the limit and stays under it. | Whether reference documents get split across many files. | Low, and reversible either way — splitting prose later is a mechanical edit plus cross-links; the risk of splitting now is a map that is harder to read than the territory. |
| `Q-024` *(hub — the Route badge scope question; the fourth thing called `Q-024`, see the note below)* | Routes are derived at **chapter** granularity, `scheme = 'chapter'`, because the `passages` table is empty and Murai's pericope ingest is another agent's work. Measured: 682 routes, 7,070 stops. Order is verse number, then the position of each place's name in the BSB text of that verse (92.3% of located mentions matched) — never a model. The `routes.scheme` column means passage-level routes can land beside these rather than replacing them. | The Route badge's sheet; how a journey is framed on the map canvas. | Low. A second derivation writes new rows under `scheme = 'passage'`; nothing existing has to move, and the chapter rows can then be dropped with one `DELETE`. |

### Note on the `Q-024` id

Two different things are called `Q-024` in this repository. The Question Hub's `Q-024` is
the walkthrough's testID contract, queued on 2026-08-29 by the walkthrough harness — that
is the id the hub will answer. The `Q-024` cited above for the four public-domain
translations, and again in `DECISIONS.md`, was written by hand and was never queued, so no
hub question carries it. The hub owner should renumber one of them; until then, cite them
by subject rather than by number.

**Correction, same day, from the client API layer.** There are now *three* things called
`Q-024`, and the note above is no longer accurate about which one the hub holds. Measured
with `node tools/question-hub/answers.mjs --all`, the hub's single `Q-024` is
**"On a bare Hermes runtime with no WebCrypto, should the anonymous device id fall back to
`Math.random`…"** — queued by the client API layer on 2026-08-29. No hub question mentions
`testID` at all, so the walkthrough harness's contract was never queued either, exactly as
the paragraph above says of the translations row. Nothing has been renumbered here: two
agents wrote a hub id into this file before the hub had allocated it, and silently
reassigning ids in a shared log would be worse than recording the clash. **Cite all three
by subject.** The row for the device-id question is in the table above.

**Fourth collision, 2026-08-29, from the places/cross-references ingest.** The hub allocated `Q-024` *again* — this time to "Route badge scope: derive routes per CHAPTER now, or wait for Murai's pericope boundaries?", queued by the places-and-crossrefs ingest engineer. Measured with `node tools/question-hub/answers.mjs --all` immediately afterwards, that is the only `Q-024` the hub holds and it is unanswered; `Q-023` is the highest answered id. So the correction above is itself now out of date: the hub's `Q-024` is neither the device-id question nor the testID contract. Nothing has been renumbered, for the reason the paragraph above gives. **Cite all four by subject.**

**Fifth collision, 2026-08-29, from the badge domain/API work — and it is not a numbering
quirk, it is a DEFECT that is destroying questions.** `tools/question-hub/ask.mjs` has now
allocated `Q-024` **six times**: to `reader-canvas-engineer`, `repair-engineer`,
`places-and-crossrefs-ingest`, `original-language-ingest`, `history-structure-ingest` and
`badge-domain-api-engineer`, at 01:42, 03:06, 03:34, 03:35, 04:07 and 04:44. The event log
(`tools/question-hub/data/questions.json`, `events[]`) records all six asks. The
`questions[]` array holds **one** `Q-024`, the most recent. Searching the file for the
places agent's route-scope wording returns nothing, and the newest snapshot in
`data/snapshots/` predates every collision, so the five earlier questions are **gone, not
merely renumbered** — the product owner will never see them. `seq` is at 162 while the
highest allocated id is 24, so the id allocator is not reading the same counter the event
log advances. **This is the highest-value thing in this note: the hub is silently losing
the fleet's questions, and every agent that "queued and kept working" since 01:42 believes
it asked something it did not.** Not fixed here — `tools/question-hub/` belongs to another
agent — but it must be fixed before the next ask, and the five lost questions re-queued.

| Question | Assumption in force | What it affects | Cost if wrong |
|---|---|---|---|
| `B-01` (badge density; the hub numbered it `Q-024`, the sixth thing so called — cite it by subject) | **Twelve badges per chapter, two per verse, with a per-kind quota of route 1 / 3D City 2 / History 2 / Root 4 / Cross-Ref 4.** The data justifies far more: Acts 16 alone has 21 place mentions, cross-references on all 40 verses and 13 aligned words in verse 14. Two per verse is the largest number that leaves a ~25-word BSB verse readable as prose on a phone; twelve per chapter keeps the chapter-foot summary list (`design-language.md` §5) to about a screen and a half. The quotas sum to 13 against a cap of 12, so the cap genuinely bites and drops the chapter's least valuable badge rather than being decorative. | `apps/api/app/modules/badges/domain/selection.py`; how dense every chapter of the reader looks. | Low. Two named constants and one quota table, plus the assertions in `tests/unit/test_badge_selection.py`. |
| `B-02` (badge anchor for verse-level claims) | **History and Cross-Ref anchor on the verse's LAST word.** Both make a claim about the whole verse rather than about one word, and tinting a mid-sentence word would assert a precision the data does not have. A consequence, tested and accepted: a verse showing a History badge cannot also show a Cross-Ref badge inline, because both want the same characters and P-04's order gives History the win. The dropped cross-reference is still reachable from the verse sheet. | `apps/api/app/modules/badges/domain/anchor.py`, `selection.py`. | Low — one anchor rule; a different rule (first word, or a "verse-level" render slot outside the text flow) changes one function. |
| `B-03` (3D City content) | **The `3d-city` badge ships the SITE, not a reconstruction.** `dataset-validation.md` §4.3 is a confirmed negative — no openly-licensed 3D reconstruction of a biblical city exists — so the payload carries the pin, the modern identification, how many identifications scholarship proposes, and where this chapter names it, all from the CC BY 4.0 gazetteer. `hasReconstruction` is `false` on every row and is the interface a commissioned model drops into later. `reconstructionId`, `eraLabel`, `summary` and `landmarks` became optional in `packages/shared`. | `packages/shared/src/badges/spatial-badge.types.ts`; the 3D City sheet's whole design. | Low to build on, high to reverse — but reversing needs an art commission, not a code change. |
| `B-04` (cross-reference relation) | **Every M2 cross-reference is `parallel`.** OpenBible publishes a vote count and no relation; labelling an unlabelled link "fulfilment" or "quotation" would be the badge inventing a claim (pillar 3). The other three values stay in the type for a source that distinguishes them. | `apps/api/app/modules/badges/domain/builders/crossref.py`. | None — a later source fills the field in. |

| `B-05` (what the Route badge may claim; queued as `Q-028`) | **The Route badge names *places named*, never travel.** `routes.scheme = 'chapter'` is derived by reading place names out of the chapter in the order the text prints them, which cannot separate a place travelled through from a place merely mentioned — Acts 16 names Jerusalem without Paul going there (16:4), records that the Spirit refused them Bithynia (16:7), and names Thyatira only as Lydia's home town (16:14). The badge therefore titles itself "Places named in this chapter", teases "16 places named in this chapter", captions its two stats `PLACES` and `SPAN`, withholds the `departure` / `destination` roles under this scheme, and deduplicates a place the chapter names twice. The inline mark still reads `[Route]`; whether it should is `Q-028`, and the recommendation taken is to keep it. | `apps/api/app/modules/badges/domain/builders/spatial.py`; `apps/mobile/src/features/sheets/spatial/model/route-view.ts`; every surface that prints a Route teaser. | Low in one direction, and free in the other: a passage-level derivation writes rows under a different `scheme` and gets its roles and its journey vocabulary back with no change to these rules. |

| `Q-024b` (theme default; the hub numbered it `Q-024`, which collides with the translations row above — cite it by subject) | **The first run opens dark, not "System".** `D-01` says dark by default and the design language is a dark cinematic canvas, but every desktop browser tested reports a light system scheme, so a switcher starting on `System` would mean most first-time web visitors never see the app as drawn. `System` remains one tap away in Settings and, once chosen, keeps tracking the OS live rather than freezing at launch. | `apps/mobile/src/theme/theme-preference.ts` (`DEFAULT_THEME_PREFERENCE`); what a reader sees on a cold start; walkthrough chapter 7, which asserts the default canvas is dark. | Low — one constant, plus one assertion in `theme-preference.test.ts`. |

| `R-01` (hub `Q-024`, attribution) | **The chapter footer prints the translation's full name, and nothing the API did not send.** `GET /translations` supplies `code`, `name`, `language` and `can_redistribute`; there is no attribution or licence string, and the client must never author one. `can_redistribute: false` renders as "Server-delivered only" — a restatement of the API's own flag, not a licence claim. When the endpoint grows an `attribution` field the footer prints it verbatim and nothing else changes. | `features/reader/components/ChapterFooter.tsx`, `TranslationSheet.tsx`. | Low — one field, one render site. |
| `R-02` (badge source) | **Resolved by M2.** `useVerseBadges` now queries `GET /badges/chapters/{translation}/{book}/{chapter}` and `model/badge-preview.ts` is deleted, as its own header said it would be the moment the real source arrived. `EXPO_PUBLIC_READER_BADGE_PREVIEW` no longer exists. | `features/reader/hooks/use-verse-badges.ts`, `features/reader/badges/`. | None. |
| `Q-024` (nested controls) | **The verse row keeps its button role; the inline pill drops its on the web only.** Both are real controls and react-native-web renders anything with `accessibilityRole="button"` as a `<button>`, so keeping both made every chapter log `<button> cannot contain a nested <button>` and raised a dev LogBox banner over the tab bar. The pill keeps its label, its hit slop and its tap target; what it loses on the web is keyboard focus, and the **chapter-end badge summary list is the keyboard-reachable route to every badge** — which is what `design-language.md` §5 built that list for. On native there is no DOM and no nesting rule, so the role stays and TalkBack still announces a button. A press on a pill also calls `stopPropagation`, so tapping one never also selects the verse beneath it. | `components/InlineBadge.tsx` (`pressableRole`), `features/reader/components/VerseText.tsx`. | Low — one function and its two callers. |
| `R-03` (highlights) | **Verse highlights live in reader-local state, while the open verse lives in `@/stores`' reader store.** The open verse is read by more than the canvas, so one store owns it; the shared store has no highlight field, and highlights are server-persisted user data whose home is the sync model (`A-03`), not a client store. They are therefore in-memory and per-chapter until that lands. | `features/reader/hooks/use-verse-selection.ts`. | Low — moving the set into the store or the API is a change to one hook. |
| `R-04` (navigator rail width) | **Superseded by `Q-024`.** The desktop navigator rail no longer exists; the clamps in `layout.contextRail` now belong to the context rail alone. | `components/split/context-rail-mode.ts`. | None. |
| `Q-024` (which rail is pinned) | **The context panel gets the pinned rail; the navigator is a sheet at every width.** A 1280 dp window minus the 232 dp sidebar leaves 1048 dp, and a 340 dp book list *and* a 320 dp context rail *and* a 460 dp reading column do not fit. The context panel is pillar 2's delivery surface and is wanted on every screenful; the picker is used once a session and is one tap from the reference. Queued as `Q-024`. | `features/reader/components/NavigatorSurface.tsx`, `ReaderScreen.tsx`, `components/split/context-rail-mode.ts`. | Low — the navigator sheet and the rail are separate components, so swapping which one is pinned is a change to `ReaderScreen`. |
| `R-05` (tablet rail is fixed) | **Between 600 and 1099 dp the context rail is fixed-width, not draggable.** `Q-006` puts the rail in scope from 600 dp, and `e2e/support/viewports.ts` puts the *split* at 1100. A tablet therefore gets the rail and no divider: there is room for the context beside the scripture, and not enough room to let a drag make either pane unreadable. The reader pane's floor in this regime is `minReaderTablet` (360 dp, a phone's column) rather than `minReader` (460), which is what makes a 768 dp tablet qualify and a 600 dp one correctly refuse. | `components/split/context-rail-mode.ts` and its test. | Low — one arithmetic rule, tested at every boundary. |
| `R-06` (Bible tab is the reader) | **Tapping Bible opens scripture at the reader's last position, rather than a plan screen.** `e2e/walkthrough/03-reader.spec.ts` states the contract; a tab that dead-ends on a stub is no route into the product. The plan content that lived there has no home yet — it belongs on Home or in a plan screen reached deliberately. | `app/(tabs)/bible.tsx`, `app/(tabs)/read/`. | Low, but the plan copy is currently unrendered and should be re-homed. |

| `L-01` (Alembic revision ids under parallel agents; the hub numbered it `Q-024`, which now collides with three other rows — cite it by subject) | **New revision ids carry a unique suffix** (`0006_lexicon`), so two agents can never write the same revision id. The cost is that `alembic upgrade head` reports **multiple heads** whenever two agents branch from the same parent, which is exactly what happened: `0003 → 0004 → 0005 → 0006_lexicon` and `0003 → 0004_history → 0005_structure` are two live heads today, and `docker compose up -d` cannot migrate until someone merges them. A plain duplicate id would have been worse — Alembic refuses to load the directory at all. The merge revision is deliberately NOT written here: agents are still landing migrations, so any merge is stale on arrival, and it belongs to whoever closes M2. This ingest was verified with `alembic upgrade 0006_lexicon`. | Every schema change from here on; whether `docker compose up -d` works. | Low — a merge revision is three lines and non-destructive. |
| `L-02` (Greek/Hebrew Unicode form) | **All original-language text is normalised to NFC at ingest.** TBESG writes σέβομαι with U+1F73 (oxia); the rest of the world uses U+03AD (tonos). They render identically and compare unequal, so without this, search finds nothing and a flashcard never matches its own lemma. NFC is canonical equivalence, and STEPBible's licence explicitly permits reformatting for an application, so this is not "changing the data". | `lexicon.lemma`, `verse_words.surface`, every original-language comparison in the app. | Low, but it must be decided once — mixing forms in one column is the outcome that cannot be repaired by a client. |
| `L-03` (`word_index` ownership) | **`verse_words.word_index` is Atlas's numbering, not TAGNT's `#nn`.** 53 KJV verses take their Greek from two NRSV verses, so both halves start at `#01` and collide; words are renumbered 1..n per KJV verse in canonical file order. For the other 7,904 verses the two numberings are identical. | Anything that tries to cite a TAGNT word by its published index. | Low — the original reference could be re-derived from the file, and no product surface uses it. |
| `L-04` (Strong's numbers no lexicon covers) | **Five dStrongs TAGNT uses are absent from TBESG** (`G0256`, `G2453`, `G3700G`, `G3700H`, `G3708`; 317 words). Their lexemes are **minted from TAGNT's own dictionary column** and attributed to TAGNT, rather than dropping the words or weakening the `lexicon` foreign key. Falling back to the undisambiguated eStrong was rejected: TBESG's `G3700` headword is ὀπτάνομαι while TAGNT says the lemma is ὁράω, so the fallback would have shown a wrong lemma with full confidence. | 317 of 142,096 Greek words; five `lexicon` rows with no long definition. | Low, and self-announcing — the loader prints the minted numbers on every run. |
| `L-05` (alignment precision over coverage) | **An English word is aligned to a Greek word only when the pairing is unambiguous in both directions.** Measured coverage is 57.8–62.8% of content words and 99.1–99.5% of NT verses; the remainder is deliberately left un-tappable. A wrong alignment shows the reader a lemma that is not behind the word they tapped, which is worse than no badge (pillar 3). | How many words in a verse carry a `[Root]` badge. | Low and reversible — relaxing the rule is a change to one pure function with its own unit tests, and coverage is re-measured on every load. |
| `H-01` (hub `Q-024` — the id collides with several rows above; cite it by subject: *Theographic dating for the History badge*) | **Theographic `Events.csv` (CC BY-SA 4.0) is ingested as the History badge's dating source, table-scoped and server-only.** `dataset-validation.md` §3.4 found no permissive alternative and §3.5 named People.csv as "the only genuine share-alike dependency" without resolving Events.csv. `Q-007` (never redistribute the database) means the ShareAlike trigger — *Sharing* Adapted Material — never fires, and `Q-016` already caps it at the New Testament. Rows live in `historical_events` and `passage_dating` with their own `source_id` and `data_sources.share_alike = true`, so exclusion is one `WHERE` clause. Recommendation queued and taken. | The History badge on 510 New Testament passages, all 51 of them in Acts; whether the enrichment corpus can ever be bundled to a device. | Low **for the server**, high **for a bundled seed**. If the answer is "no share-alike anywhere", the two tables are dropped and the badge shows rulers with no "you are here" marker — no other table is touched, because nothing blends them. |
| `H-02` (Murai's Japanese column) | **The Japanese summary column is never read.** The provenance note calls it "the primary summary" and flags only the *English* column as carrying copyrighted verse text. It is wrong: the Old Testament sheets quote a Japanese translation in that column verbatim, which is no more Murai's to license than the NAB is. The product is English-only, so nothing is lost today. | `scripts/murai_parser.py`; what a future Japanese localisation could reach for. | None now. If Japanese ships, that column needs its own copyright review before it is touched — it is not a free win. |
| `H-03` (one date per passage) | **`passage_dating` picks the most *specific* overlapping event, not the one covering the most verses.** Theographic carries 32 umbrella events; "Second Missionary Journey" spans Acts 15-18, and ranking by raw overlap gave every passage in those four chapters the umbrella's AD 46 instead of the AD 47 of the episode on the page. Ranking by the fraction of the *event* inside the passage puts the episode first and keeps the umbrella as the fallback. `confidence` is coverage, not certainty, and the `rationale` string says so in the words the reader sees. | The year the History badge prints on 510 passages. | Low — one `ORDER BY`, and the whole table is rebuilt by re-running the loader. |

| `S-01s` (Route stat strip; the hub numbered it `Q-024`, which now collides with several rows — cite it by subject: *the Route sheet's stat strip*) | **The strip prints a DERIVED straight-line distance, the stop count and the longest leg, and no duration.** `RoutePayloadOut` sends none of the three. Distance follows from the gazetteer pins, so it is computed and captioned `STRAIGHT LINE`; duration does not follow from anything in `data/raw/`, so it is absent rather than estimated. The mockup's own figures are unreproducible: `image1.png` says "125 Miles by Sea" for Troas to Philippi, and the straight line through Samothrace and Neapolis is already ~157 miles, which a sailed track can only exceed. A stat whose value cannot be computed is dropped, not dashed. | `features/sheets/spatial/model/route-view.ts`; every Route sheet's stat strip. | Low. Three named captions and one function; a sourced sailing distance fills the gap without moving anything else. |
| `S-02s` (map renderer) | **`react-native-svg`, not Skia.** Measured, not preferred: 96 SVG nodes for the whole 20-stop Acts 16 map with the 127 coastline rings as ONE `fill-rule: evenodd` path; 0.92 ms median / 2.02 ms p95 to project and stringify the visible coastline; 7.6 ms median frames through the draw. Skia's advantage is per-frame raster work, of which there is none, and its web cost is a ~2.9 MB CanvasKit WASM binary plus an async gate before first paint — on a first-class web target (`T-01`). Full comparison in `features/sheets/spatial/README.md` §2. | The whole spatial feature; any later Discover-tab map. | Moderate to reverse — `MapSurface` is one component and the geo layer is renderer-agnostic, but every child would be rewritten. |
| `S-03s` (basemap source) | **Natural Earth 1:50m land and lakes, public domain, vendored and cropped** (`data/raw/natural-earth/`, `tools/geo/build-basemap.mjs`, 45,921 bytes). `M-01` forbids a tile provider and the repo held no polygons; `openbible-geocoding/PROVENANCE.md` had deliberately declined OpenBible's partly-ODbL geometry slice. Public domain is what makes bundling lawful under `Q-007`, which is what makes the map work offline. 1:110m was rejected on evidence: Samothrace, an Acts 16:11 waypoint, does not survive it. | `features/sheets/spatial/geo/basemap*`; whether the map needs a network. | Low — one generator and one JSON. |
| `S-04s` (route line geometry) | **A centripetal Catmull-Rom spline at 0.5 tension, which interpolates its knots.** The line therefore passes exactly through every pin, so the drawing makes no geographic claim the gazetteer does not support; a straight polyline through twenty Anatolian stops reads as a fault, and an approximating spline would move the line off its own pins. Centripetal parameterisation is what stops a tight cluster (Mysia, Bithynia, Troas within two degrees) throwing a cusp. | `features/sheets/spatial/geo/route-path.ts`. | None — one parameter, and `smoothing: 0` reproduces straight legs exactly. |
| `S-05s` (map label density) | **Labels are decluttered greedily, departure and destination first, and a colliding label is DROPPED rather than moved.** Twenty labels on a phone-sized map cover the coastline. Moving a label away from its pin is how a map attaches a name to the wrong dot; the pin is still drawn and the name is still in the stop list underneath. | `features/sheets/spatial/components/label-declutter.ts`. | None — one pure function with its own tests. |
| `S-06s` (site framing) | **The `[3D City]` map is a LOCATOR at zoom 6.2, with a coordinate graticule and a scale bar.** Chosen by looking: at 8.4 an inland site such as Lystra has no coastline in frame and the map reads as a failed render. The graticule is the honest depth cue that replaces the reconstruction `Q-008` says we cannot have — hill shading, roads or city blocks would all fill the space with geography we do not hold. | `features/sheets/spatial/components/CitySiteMap.tsx`, `geo/graticule.ts`. | Low — two constants. |
| `S-07s` (the SVG stub makes attribute assertions vacuous) | **Component tests must not assert SVG geometry.** `apps/mobile/src/testing/react-native-svg-stub.tsx` renders every element as a `View` and forwards only `testID` and the ARIA props, so `getAttribute('stroke-dashoffset')` returns `null` — and `Number(null)` is `0`, which is exactly what a passing reduced-motion assertion expects. Such a test passes whether or not the component works; one was written here, caught, and replaced by a composition test over the pure functions (`hooks/draw-progress.test.ts`). **Any agent writing a component test against `react-native-svg` needs to know this.** | Every component test touching SVG, in any feature. | None to act on; high if unknown. |

### Repair round 1 — the M2 walkthrough's findings

| Assumption | Decision taken | What it touches | Cost to reverse |
|---|---|---|---|
| `Q-025` (the `[3D City]` mark) | **The reader-facing label is `[Site]`.** `DECISIONS.md` §4 records 3D City as one of the two genuinely dataset-less badges — the nearest reconstruction corpus is CC BY-NC-ND, which fails on NonCommercial and again on NoDerivatives — so the sheet shows the gazetteer record and says so in a sentence, and its own eyebrow already read `SITE`. The mark now promises what the sheet delivers. The **wire kind stays `3d-city`** and the theme key stays `city3d`: neither is reader-facing, and renaming a published discriminator for a label would be a breaking change for a cosmetic gain. Queued as `Q-025`; recommendation taken. | `components/InlineBadge.types.ts`, `packages/shared/src/badges/badge-kind.ts`, and their tests. | None — two strings. |
| `Q-026` (badge hues vs the verse number) | **`design-language.md` §2's hue table stands as written; the overlap is recorded rather than repainted.** Verse numbers, `[Site]` and `[Cross-Ref]` all measure `rgb(240,180,41)` in dark and `rgb(138,91,11)` in light, which §8.2 ("gold means you, cyan means the system") reads as a mixed meaning — but §2 assigns gold to place names *and* to verse numbers *and* to those two badges, so the document contradicts itself exactly as conflict `C-4` did. Contrast is not at issue (5.38:1 to 6.04:1 in light, above AA). A pill has a bracketed mark, a border and a fill; a verse number is a bare superscript numeral in a fixed gutter — they are not confusable in form, only in hue. Queued as `Q-026`; recommendation taken pending the owner. | `theme/colors.ts`, `theme/light-colors.ts`. | Low — two entries in `badgeColorsFor`, and the contrast tests re-run. |
| `L-06` (Root badge scope) | **The `[Root]` badge is New Testament only, deliberately.** `verse_words` and `verse_word_alignments` hold book numbers 40-66; Genesis 1, Ruth 1, Psalms 23, Jonah 1 and Obadiah 1 each return zero Root badges, and the 8,021 loaded Hebrew lexicon rows are unreachable from the reader. The word layer is STEPBible **TAGNT**, which is the Greek NT; the Hebrew equivalent (TAHOT) is not in `data/raw/`, and ingesting it is a download, a second parser and a Hebrew-to-English alignment that the Greek gloss-stem matcher does not generalise to. `P-01` already scopes full depth to Acts, so a Hebrew Root badge would be depth in a book with none of the other four. Recorded the way `Q-016` records NT-only dating. **Consequence:** right-to-left rendering is exercised only by the synthetic `HEBREW_ROOT_PROBE` at `/spike/textual-sheets` — tested, but not reachable from scripture. | `builders/root.py` (header), the Root badge's coverage, RTL confidence. | High — a dataset acquisition and an alignment pass, not a code change. |
| `B-05` (malformed lexicon glosses) | **A gloss whose last whitespace token reduces to one or two letters English does not use as a word is rejected, and the same row's definition is used instead.** TBESG ships `G1874` as "to listen ro" and `G20062` as "inerudite et"; both were ingested faithfully and served to the reader as sourced claims. Measured across all 11,035 TBESG entries the rule matches exactly those two and no correct gloss — including the many that legitimately end in a short preposition ("to sit on", "to go out"), which a wider "short trailing word" rule swallowed by the hundred. Nothing is invented: the fallback is the same row, same source, same licence, and a row with neither usable field builds no badge. | `apps/api/.../domain/gloss.py`, `builders/root.py`. | None — one pure function with its own tests. |
| `B-06` (evidence chips) | **A citation chip is dropped when the attribution strip below it already prints the same sentence.** Every M2 citation is `source_citation(...)`, whose label *is* the dataset's attribution line, so a Root badge printed one sentence four times — two identical chips (TBESG and TAGNT are two files of one project) and two strip lines. For most badges nothing survives the filter, which is the honest answer: the sources are the evidence, and they are printed once. A citation carrying something the strip cannot say still renders — Acts 16:15's cross-reference has one. | `features/reader/badges/badge-evidence.ts`, `BadgeEvidence.tsx`. | None — one pure function. |
| `R-07` (the tablet reading measure) | **The gutter and the scripture type step follow the reading PANE's width, not the window's form factor.** A 768 dp tablet is a tablet by every rule in `breakpoints.ts`, and its scripture pane is 408 dp once the 80 dp nav rail and the 280 dp context rail are taken off — so tablet type in a tablet gutter produced a 306 dp column, narrower than the same app's phone column and in larger type, at roughly 28 characters a line. Below `layout.contextRail.minReader` (460 dp) the pane now reads like a phone: 16 dp gutter, `sm` step. Measured in Chrome at 768 dp: 306 dp → 338 dp of column at a smaller step; 1024 dp and 1280 dp are unchanged. The alternative — refusing the rail at 768 — was rejected because `Q-006` puts the rail in scope from 600 dp. | `features/reader/model/reader-canvas.ts`, `hooks/use-reading-canvas.ts`, `components/split/context-rail-mode.ts`. | Low — two pure functions and their tests. |
| `R-08` (the badge sheet seam) | **`BadgeSheetHost` is the single place `features/reader` and `features/sheets` meet, and every body is drawn with `chrome="body"`.** The provider had never been mounted, so all five badges opened onto a pill, a reference, a teaser and a source list. Bodies are asked for `body` chrome because `BadgeDetail` has already drawn the mark, the reference, the teaser and the `AI-05` strip; drawing them again printed the badge's name twice and its licences twice. The textual folder gained the `chrome` prop the spatial folder already had. | `features/sheets/BadgeSheetHost.tsx`, `app/_layout.tsx`, `TextualSheet` and its three sheets. | Low — one file registers all five; a sixth badge is one entry. |
| `H-04` (`Q-015` in the reader) | **The inline attribution is a mark beside the teaser, in sentence case, wherever the teaser is printed.** The `[History]` teaser carries Hajime Murai's pericope title; the reader printed it bare in the open sheet and in the chapter-end summary while the API was already sending `interpretiveClaim` and `attributedTo`. The mark is set in sentence case rather than the uppercase metadata band on purpose: §3 reserves that band for data (references, Strong's numbers, dates), `Q-015` spells the phrase "Murai's reading", and `HistorySheet`'s own `CaveatNote` heading two lines below is already uppercase — setting both the same way printed the identical shout twice in one surface. | `features/reader/badges/badge-claim.ts`, `BadgeClaimMark.tsx`, `BadgeDetail.tsx`, `BadgeSummaryRow.tsx`. | None — one pure rule and a short component. |
| `S-08s` (site map framing) | **The `[Site]` map steps its camera out until at least twelve coastline vertices are drawn inside the frame.** Zoom is degrees per *pixel*, so the fixed 6.2 that framed Jerusalem well left Lystra as an empty graticule with one pin in the 232 dp tablet rail — a grey blob that reads as a failed render. Twelve vertices of a 0.05-degree simplified coastline is a line crossing the view rather than a corner grazing it; one vertex was measured to be exactly what the first step out produced, and it draws a mark a reader would take for an artefact. Coastal sites are unchanged (Jerusalem 18 at the preferred zoom, Thyatira 12); Lystra and Iconium widen to 5.2 in the rail and 5.53-5.87 on a phone. | `features/sheets/spatial/geo/site-framing.ts`, `geo/basemap.ts`, `components/CitySiteMap.tsx`. | Low — one threshold and one loop, both tested. |
| `T-14` (the Question Hub's id allocator) | **`nextId` reads the highest number in a prefix family and steps over anything taken, instead of counting.** Counting is only correct for a contiguous, uniformly padded family, and the real file is neither — `Q-01`..`Q-04` were seeded unpadded beside `Q-005`+, so 23 `Q-` questions existed while the highest was already `Q-024`. Two agents in a row were handed `Q-024`, and because `/api/ask` upserts by id each silently overwrote a question a human had already answered. This log already carries four rows apologising for that collision. The question damaged during this round was restored verbatim from `data/snapshots/`. The hub was restarted and the fix verified live: the next `ask` landed on `Q-027`, where the old allocator would have returned the already-taken `Q-026`. | `tools/question-hub/lib/questions.mjs`, and every future `ask.mjs` call. | None — the allocator is strictly safer, and a regression test now pins it. |
| `S-09` (a demonym as a place name, `Q-029`) | **A place is listed when the chapter spells any published spelling of it, and the pin is labelled with the words the verse uses — demonyms included, pending `Q-029`.** The Route badge now verifies every name against the chapter it lists it under; measured across all 682 derived routes, 752 of 5,083 listed names were absent from their chapter before the fix and 0 are after. The residue is ~41 waypoints of 4,399 where the only spelling the BSB uses is a demonym: Acts 27:2 is "an Adramyttian ship", so the pin reads "Adramyttian", and elsewhere "Ninevites", "Medes", "Jews", "Philistines". That is a different class from the Greece defect — the word is on the reader's page and the pin is at the right city — but it is still a word about a people under a heading about places. No dataset we hold separates the two: OpenBible publishes "Alexandria", "Alexandrian" and "Alexandrians" as three undifferentiated spellings, and `instance_types` is a vote across ten translations rather than a fact about one. Every morphological rule tried also deleted genuine variants — Beth-shean for Beth-shan, Azotus for Ashdod, Euphrates for River — so suppressing them needs hand curation of about a dozen entries. Queued as `Q-029`; recommendation taken. | `badges/domain/builders/place_support.py`, `builders/route.py`. | Low — one predicate and a deny-list, if the owner wants them gone. |

### Repair round 2 — where a printed number did not match the thing beside it

| Assumption | Decision taken | What it touches | Cost to reverse |
|---|---|---|---|
| `L-07` (which Strong's number the Root badge counts) | **Count across all senses of the SIMPLE number, rather than publish the disambiguated one.** The bug was a pairing: `lexicon_usage` was keyed per dStrong (`G2424K`) while the payload published `simple_strongs` (`G2424`), so 26 badges printed a concordance number beside the frequency of one sense of it. Two fixes were available, and the brief allowed either. Publishing `G2424K` was rejected: no concordance carries it, the definition on the same sheet is already Dodson's entry for the simple number, and the headword is Ἰησοῦς for all five senses — so "This word occurs once in the whole of the Greek New Testament" would still be false of the word on the chip. Counting at the simple level makes every line of the sheet describe one identifier. The visible cost is accepted: 88 sense-split lexemes now measure above the twelve-occurrence bar and earn no badge, so Colossians 4:11's Ἰησοῦς badge is gone and παρηγορία — a genuine hapax — takes its place. The sense survives where it belongs, in the gloss, which still comes from the disambiguated row. | `db/versions/0009_simple_usage`, `scripts/lexicon/word_layer_writer.py`, `badge_sql.CHAPTER_WORDS`, the Root badge's coverage. | Low in code — one join key and one `GROUP BY` — but it is a reload, and it changes which word is badged in every affected verse. |
| `S-10` (the wire name of the 3D City count) | **`canon_verse_count` is renamed `named_verse_count` on the wire, not just corrected in value.** The number changed meaning: it counts `place_mentions` of kind `name` rather than every mention. A client holding a cached payload under the old key would keep rendering the old claim under a name that no longer describes it, and `verse_count` is precisely the name that invited the wrong reading in the first place. The stat caption moves with it, `VERSES IN CANON` → `VERSES NAMING IT`, because the caption is the claim the reader actually reads. | `places.named_verse_count`, `City3dPayloadOut`, `packages/shared`, `city-view.ts`, the spatial fixtures. | Low — a mechanical rename with a migration either way; there is no released client to break. |

## Standing constraints (not assumptions — stated by the human)

- `A:\Work\gt\ControlSight` and `A:\Work\spark\spark-app` are **read-only**. Never modify.
- The OpenRouter key holds **$4.57** (measured). The **$2 ceiling is kept as a deliberate
  self-imposed cap** (`Q-020`, `ABSOLUTE_MAX_CEILING_USD`). Cheap open-weight models only;
  no large frontier models. Every call goes through the spend guard and cache.
- The end state is production-ready, verified by repeated click-through walkthroughs
  of the real UI, not by unit tests alone.
- **The Question Hub is a first-class second project**, not a throwaway tool. It is
  engineered, tested, and improved continuously to the same standard as the app.

## Resolved

Answered by the product owner on 2026-08-28. These are now **decisions**, not assumptions.

| Question | Decision | Change from the assumption |
|---|---|---|
| `P-01` | Whole-Bible reader shell + full multimodal depth only for Acts. | None — assumption confirmed. |
| `P-02` | Hybrid: the PRD's screens and visual language win; the Flutter app's reader/chat *behaviour* is the quality bar. | None — assumption confirmed. The port map's §7 "behaviours worth preserving" is therefore binding. |
| `D-01` | Dark cinematic by default. **Light mode must actually be built and shipped**, not merely made possible. | **Changed.** The assumption deferred light mode; it is now a real deliverable. Every component must be verified in both themes, and the walkthrough must cover both. |
| `T-01` | **Android device + Web.** | **Changed.** Web was assumed to be a testing convenience (`T-04`); it is now a first-class target. This rules out native-only libraries and doubles the UI QA surface. iOS is out of scope. |
| `B-01` | Keep FastAPI, restructured to the layering rules. | None — assumption confirmed. |
| `A-01` | Anonymous device id now, real accounts later. | None — but the port map's risk #9 stands: do not port the prototype's hardcoded `dev-user` stub. Build the identity seam properly. |
| `S-01` | Multiple open translations with a switcher. | None. ESV in the mockups is **not** licensed and must not ship; the reader chrome should show the active translation code generically. |
| `AI-01` | Pre-compute all 28 chapters of Acts (~$0.05). | None — assumption confirmed, now with measured costs behind it. |
| `M-01` | Custom stylised map from GeoJSON. **No tile provider, no Mapbox token.** | None. All route/city rendering must be self-contained; no runtime tile fetches. |
| `AU-01` | **Stubs.** Full player UI with a few hand-made sample tracks; no generation pipeline. | **Changed.** The assumption was open-TTS generation. Audio drops down the priority order; build the player UI and the seam, not the pipeline. |
| `W-01` | Take the recommendation, log the assumption, keep moving. **Plus**: the Question Hub becomes a first-class second project. | **Extended.** See the standing constraint above and `docs/architecture/hub-platform.md`. |
