# Decisions — Question Hub state

`98/98` answered · `0` blocking still open.

> Generated by `tools/question-hub/answers.mjs`. Do not edit by hand.

## 1 · Scope & Product Truth

### ✅ `P-01` What is the actual deliverable you want at the end of this run?

_Sets how far the fleet pushes. Everything downstream scales from this answer._

**Answer:** Whole-Bible reader shell + full multimodal depth only for Acts

### ✅ `P-02` Is this a rewrite that must preserve the current Flutter app's features, or a fresh build against the PRD?

_The Flutter app (Lampstand UI: warm paper, green/amber, reader + ask + library + sources) and the PRD (dark cinematic Atlas Bible: 5 tabs, 3D maps, streaks) are almost entirely different products. I need to know which one is the target._

**Answer:** Hybrid — keep the Flutter reader/chat behaviour and quality bar, but the PRD's screens and visual language win

### ✅ `P-03` Which of the 5 PRD tabs must be genuinely working (not stubbed) for you to call this a success?

_Lets me rank the build order and decide where to spend the deep-polish budget._

**Answer:** Bible (Reader + inline badges + sheets) | Journal (reflections + milestones)

### ✅ `P-04` Of the 10 inline badge types in the PRD, how many should I build for real in this run?

_Each badge type is a distinct data model + sheet UI + content pipeline. Ten is a lot; three done beautifully may beat ten done thinly._

**Answer:** The 5 highest-impact (Route, 3D City, History, Root, Cross-Ref)

_Accepted in bulk — the human endorsed the default but did not deliberate on it._

### ✅ `P-05` Name one thing that, if it isn't spectacular, means the whole run failed.

_I want a single north-star quality bar to hold every agent's output against._

**Answer:** Ux and Ui. Enrichment accuracy. Chat accuracy and theological guidance.

### ✅ `P-06` Is the 'Aletheia' vs 'Atlas Bible' naming settled?

_Affects app name, bundle id, package names, brand strings, and the entire design system's naming._

**Answer:** Undecided — use ATLAS_BIBLE everywhere and keep it swappable via one constant

_Accepted in bulk — the human endorsed the default but did not deliberate on it._

### ✅ `P-07` Anything in the PRD you already know is wrong, dropped, or has changed since it was written?

_The doc has open review sign-offs from 7 people. I'd rather not build something you've already rejected._

**Answer:** Not rejected yet. Can you turn those into decisions i make here just in case. There were already questions addressing them so make sure you don't collide with those.

### ✅ `P-08` The PRD comment asks: 'can we build this as an expandable 3D bible vs today's map'. Should I pursue that?

_David's comment implies a fully explorable 3D world rather than a 2D/2.5D map with pins. Very different engineering cost._

**Answer:** Build the 2.5D version but behind an interface that can swap to full 3D later

_Accepted in bulk — the human endorsed the default but did not deliberate on it._

## 2 · Design & Brand

### ✅ `D-01` Confirm the visual direction: the PRD mockups are near-black, cinematic, gold + cyan, serif scripture. Is that the target?

_The existing Flutter app is the opposite — warm cream paper, green/amber, light mode. I need to commit to one design system before any component is written._

**Answer:** Dark by default, light mode later (build tokens so it’s possible)

**Note:** Light mode must ACTUALLY be built and shipped, not merely made possible by the token structure. Dark stays the default.

### ✅ `D-02` How closely should the built UI match the mockup screenshots?

_Pixel-matching the mockups constrains layout; treating them as mood boards frees us to make it feel better in motion._

**Answer:** Other: Take inspiration. But make it a professional app.

### ✅ `D-03` Which typefaces should I use? (Mockups read as a classical serif for scripture + geometric sans for UI.)

_Font choice is load-bearing for the whole feel and needs deciding before the design system lands._

**Answer:** Serif: Source Serif 4 (as in the Flutter app)

### ✅ `D-04` Do you have brand assets (logo, the gold globe mark, colour hex values) or should I generate them?

_The mockups show a specific gold wireframe globe logo. If it exists as a file, I'll use it._

**Answer:** Generate/approximate them, I'll replace later

### ✅ `D-05` How much motion do you want?

_The PRD says 'cinematic' and 'glassmorphic sheets'. Motion budget affects performance targets and library choice._

**Answer:** Other: No excessive glass stuff tho textures would be nice

### ✅ `D-06` Accessibility bar for v1?

_Dark gold-on-black at small sizes can fail WCAG AA. Deciding now avoids a repaint later._

**Answer:** AA for text contrast + dynamic type support; screen reader best-effort

## 3 · Platform & Distribution

### ✅ `T-01` Which platforms must actually run?

_Expo can target all of these, but 3D maps, audio, and haptics behave very differently per platform. This decides the test matrix._

**Answer:** Android (device) | Web

### ✅ `T-02` What device(s) will you personally test on? (model + OS version)

_So I tune performance budgets and the E2E walkthrough to your real hardware._

**Answer:** Windows laptop and my android phone poco m6 5G

### ✅ `T-03` Expo managed workflow, or do we expect to need custom native modules?

_Mapbox/3D/audio-mixing may force a development build. Knowing early avoids a painful migration._

**Answer:** Development build (expo-dev-client) from day one — assume native modules

### ✅ `T-04` Should the web build be a first-class target or a convenience for testing?

_Making web first-class rules out some native-only libraries and roughly doubles the UI QA surface — but it makes my automated click-through testing far easier._

**Answer:** First-class — real users will use the web app

### ✅ `T-05` Do you want EAS Build / app store submission set up in this run?

_Requires an Expo account and Apple/Google credentials. Not something I can do without you._

**Answer:** Config files only, I'll run the builds

## 4 · App Architecture

### ✅ `T-10` Router: Expo Router (file-based) or React Navigation directly?

_Expo Router gives deep-linking and typed routes for free, which matters for 'tap a city → open Bible at that verse'._

**Answer:** Expo Router (file-based, typed routes)

### ✅ `T-11` Styling approach?

_Needs to support a token-driven dark theme, work on native + web, and stay fast in long scripture lists._

**Answer:** StyleSheet + a typed design-token module

_Accepted in bulk — the human endorsed the default but did not deliberate on it._

### ✅ `T-12` Client state management?

_ControlSight uses Zustand; I'd mirror that unless you prefer otherwise._

**Answer:** Zustand (matches ControlSight)

### ✅ `T-13` Server-state / caching layer?

_Scripture, enrichment, and AI responses all need caching and offline persistence._

**Answer:** TanStack Query + persisted MMKV cache

### ✅ `T-14` TypeScript strictness?

_ControlSight's rules demand complete, strict type annotations. I want to know how hard to push._

**Answer:** Maximum — strict, noUncheckedIndexedAccess, no `any`, ESLint blocks the build

### ✅ `T-15` Monorepo layout?

_ControlSight is a plain multi-app repo with ONE root manifest and no workspaces. That rule was written for its stack; a JS/TS repo may want workspaces for shared types._

**Answer:** pnpm workspaces — apps/mobile, apps/api, packages/shared-types, packages/ui

## 5 · Backend & Data

### ✅ `B-01` Keep the Python/FastAPI backend, or rewrite the backend too?

_You said 'rewrite the Flutter app into React Expo' — that's the client. The existing server is FastAPI + Chroma + Postgres. Keeping it is faster; a TypeScript backend gives end-to-end shared types._

**Answer:** Keep FastAPI but restructure it to the ControlSight layering standard

### ✅ `B-02` Database?

_The existing docker-compose already runs pgvector/pg16 on port 5435, and there's a Railway Postgres URL in .env.local._

**Answer:** Postgres + pgvector in Docker locally, Railway/Neon for anything shared

### ✅ `B-03` How much should the backend be dockerised?

_You gave me freedom to dockerise. Full compose means one command spins up API + DB + worker + seed._

**Answer:** Docker compose with hot-reload bind mounts so I get both

### ✅ `B-04` Do you want a migrations tool wired up (Alembic / Drizzle) or is a plain SQL schema file enough?

_Migrations are the difference between a demo and something you can actually evolve._

**Answer:** Yes — proper migrations from commit one

### ✅ `B-05` Where does the pre-computed enrichment data live?

_The PRD's passage JSON (spatial/temporal/structural) is the heart of the app. Its home decides the whole content pipeline._

**Answer:** Both — bundled JSON as the offline seed, Postgres as the source of truth

### ✅ `B-06` Is there existing enrichment data I should look at? (The Flutter repo has a `bible-enrichment/` folder.)

_If content already exists I should build to its shape rather than inventing a new one._

**Answer:** Yep some should be there take a look


## 6 · Auth & Accounts

### ✅ `A-01` Authentication for v1?

_The Flutter app uses Clerk and there are Clerk test keys in .env.local. Auth touches every screen, so it needs deciding early._

**Answer:** Anonymous device id now, real accounts later

### ✅ `A-02` If/when there is auth, which sign-in methods?

_Apple Sign-In is mandatory on iOS if any other social login exists._

**Answer:** Guest / no account

### ✅ `A-03` Does user data need to sync across devices in v1?

_Local-only streaks and journal entries are dramatically simpler. Sync means conflict resolution._

**Answer:** Yes — streaks, journal, and flashcards sync

## 7 · Scripture & Licensing

### ✅ `S-01` Which Bible translation ships in this build?

_The mockups show ESV, which is licensed and cannot be redistributed without a Crossway agreement. The PRD itself says launch on BSB or WEB._

**Answer:** Multiple open translations with a switcher

### ✅ `S-02` Do you already have Bible text data, or should I source it? (The Flutter server has `loaders/load_verses.py` and a `db/` folder.)

_Avoids me re-downloading and re-parsing something you've already loaded._

**Answer:** Enrichment data is limited. But verses should be there please examine.

### ✅ `S-03` Full Bible text in the build, or just Acts?

_Full Bible is ~4.5MB compressed per translation — very shippable — but the reader UX (book/chapter picker) differs a lot._

**Answer:** Full Bible — the reader should navigate anywhere

### ✅ `S-04` Greek/Hebrew word roots — data source?

_The [Root] badge needs Strong's numbers and lemma data mapped to verses._

**Answer:** Strong's Concordance (public domain) + an open interlinear

## 8 · AI & Generative Features

### ✅ `AI-01` CORRECTED: the OpenRouter key actually holds $4.57 remaining (not $2), and pre-computing enrichment for the WHOLE New Testament costs about $0.40. What scope do you want?

_Measured, not estimated: Acts 16 = $0.0017, all 28 chapters of Acts = $0.046, whole NT = $0.40 (~$0.60 with retries). Budget is NOT the constraint — review time is. Live chat is the real ongoing cost at ~$0.000385/turn (~11,800 turns of credit). Please also confirm whether $2 was a stale note or a deliberate self-imposed cap._

**Answer:** Pre-compute all 28 chapters of Acts ($0.05) — matches the MVP roadmap

### ✅ `AI-02` Which open-weight models should I default to on OpenRouter? (Best bang-for-buck, no large frontier models.)

_You set the constraint; I want your preference before I wire the model registry._

**Answer:** Other: Whatever spark app was using (flutter one)

### ✅ `AI-03` Should I add a hard spend guard so the fleet can't burn the $2?

_Automated testing loops can quietly drain a key. A budget ceiling + request accounting prevents that._

**Answer:** Yes, plus cache every AI response to disk so repeat runs cost nothing

### ✅ `AI-04` The PRD says 'powered by NotebookLM' and 'Zero-Hallucination AI'. NotebookLM has no public API. How should I read that?

_This is the single biggest ambiguity in the PRD. It changes the whole Studio tab's architecture._

**Answer:** It means NotebookLM-*style* — build our own grounded RAG with strict citations

### ✅ `AI-05` How strict should citation enforcement be?

_'Zero-hallucination' is a strong claim. I can enforce it structurally — refuse to render any claim lacking a verse/source anchor._

**Answer:** Structural — every sentence must carry a source anchor or it isn't shown

### ✅ `AI-06` Theological guardrails — whose lane?

_The PRD demands no denominational bias. That's a prompt-engineering and eval problem, and I want to know how conservative to be._

**Answer:** Present multiple traditions' views where they differ, never pick one

### ✅ `AI-07` Should AI run at request time in the app, or only in an offline build-time pipeline?

_The PRD's cost strategy says pre-compute everything. But grounded chat is inherently live._

**Answer:** Pre-computed for all badge/sheet content; live only for the Studio chat

### ✅ `AI-08` Do you want an eval suite for the AI outputs?

_Without evals, 'grounded' is a vibe. With them, it's a number I can hold agents to._

**Answer:** Yes — golden-set evals for citation accuracy and factual grounding, run in CI

## 9 · Maps, 3D & Visualisation

### ✅ `M-01` Mapping stack — do you have a Mapbox account/token?

_The PRD names Mapbox specifically. Its RN SDK needs a token and a dev build. Without a token I need a different plan._

**Answer:** No — build a custom stylised map from GeoJSON, no tile provider at all

### ✅ `M-02` How should the 'cinematic 3D terrain' actually be rendered?

_The mockups show gorgeous 3D relief. Real 3D terrain in RN is heavy; a stylised pre-rendered approach can look nearly as good for far less._

**Answer:** You decide after prototyping two of these side by side

### ✅ `M-03` 3D city reconstructions (the 'Philippi' / 'Solomonic Jerusalem' assets) — realistic for this run?

_These are art assets, not code. Without a 3D artist they'd have to be AI-generated stills or procedural._

**Answer:** AI-generated stills now, but build the sheet so real 3D can drop in

### ✅ `M-04` Where does the geographic data (ancient place coordinates) come from?

_Needed for every route, pin, and flythrough._

**Answer:** Other: Let's have agents look into this.

## 10 · Audio

### ✅ `AU-01` The dual-host AI podcast is central to the PRD's habit loop. How real should it be in this run?

_ElevenLabs costs money and needs a key you haven't given me. Options range from real TTS to a convincing placeholder._

**Answer:** Ship the full player UI with a few hand-made sample tracks, no generation pipeline

**Note:** Stubs for now. Revisit once the reader and badges are real.

### ✅ `AU-02` The 'Atmosphere Mixer' (independent voice + background music sliders) — build it?

_It needs two simultaneous audio streams with independent gain. Doable with expo-audio but it's real work._

**Answer:** Yes — it's a signature feature, build it properly

### ✅ `AU-03` Synchronised 'glowing lyrics' (word-level highlight as audio plays) — in scope?

_Needs word-level timestamps. Achievable with forced alignment, but it's a meaningful chunk of work._

**Answer:** Yes — verse-level highlight is enough

### ✅ `AU-04` Background audio + lock-screen controls?

_Requires native config and changes the audio architecture. Much cheaper to decide now than retrofit._

**Answer:** No — foreground only for v1

## 11 · Habit & Gamification

### ✅ `G-01` What exactly counts as 'completing' a day?

_The PRD says all 3 steps (Listen, Explore, Reflect). I want to confirm before building the state machine._

**Answer:** Just opening and reading the passage

### ✅ `G-02` How is a 'day' defined?

_Timezone handling is a classic source of streak bugs. Deciding this prevents a whole bug class._

**Answer:** Local midnight with a 4am grace boundary (late-night readers keep the streak)

### ✅ `G-03` Grace Savers — how are they earned?

_The PRD says 'up to 2 per month through consistent engagement' but doesn't define the mechanic._

**Answer:** Earn by completing bonus activities (quiz streaks, extra chapters)

### ✅ `G-04` Push notifications for the daily drop?

_Onboarding step 3 configures a notification time. Needs expo-notifications and permission flows._

**Answer:** Other: Also allow server pushes

### ✅ `G-05` Is there a fixed 30-day Acts reading plan, or does the user pick their own passage?

_Onboarding says 'choose Testament, Book, Chapter' but the roadmap says a fixed 30-day Acts journey. These conflict._

**Answer:** Both — offer the curated Acts plan or a custom plan

## 12 · Studio

### ✅ `ST-01` Which Studio artifacts must be real (generated, not mocked)?

_Each is a separate generation pipeline plus a viewer UI._

**Answer:** Executive Briefings (chapter summaries) | Grounded chat

### ✅ `ST-02` Spaced repetition for flashcards — real algorithm or simple review list?

_FSRS/SM-2 is genuinely better but adds scheduling state and a lot of tests._

**Answer:** Real SM-2/FSRS scheduling

### ✅ `ST-03` 'Custom Notebooks' — can users add their own sources (upload a PDF, paste notes) and ground the AI on them?

_This is the actual NotebookLM feature. Big scope, and it changes the RAG architecture from fixed-corpus to per-user._

**Answer:** Design for it, build it later

## 13 · Journal & Privacy

### ✅ `J-01` The PRD says journal entries are 'encrypted at rest'. How far should I take that?

_Options range from OS-level storage encryption to real client-side E2E encryption where the server can't read entries._

**Answer:** Client-side encryption before any sync, server holds ciphertext only

### ✅ `J-02` Any analytics/telemetry in the app?

_The KPIs section wants D7/D30 retention and 'modality tap depth', which requires event tracking._

**Answer:** Local-only metrics I can view in a debug screen

## 14 · Offline & Performance

### ✅ `O-01` Must the app work fully offline?

_The PRD's cost strategy leans on offline caching. Full offline changes the data layer substantially._

**Answer:** Scripture + enrichment offline; AI and audio need a connection

### ✅ `O-02` Performance target?

_Gives me a number to hold the fleet to, and something the E2E tests can assert._

**Answer:** Set sensible budgets yourself and enforce them

## 15 · Testing & Quality

### ✅ `Q-01` Which test layers do you want?

_You asked for end-to-end click-through loops. I want to confirm the full pyramid beneath them._

**Answer:** E2E on web (Playwright) | Component (React Native Testing Library) | API integration (pytest/supertest) | Unit (Vitest/Jest)

### ✅ `Q-02` Is TDD genuinely mandatory here (tests before implementation), as in ControlSight?

_It roughly doubles agent output volume but catches far more. Your call._

**Answer:** Tests alongside implementation, before the feature is called done

### ✅ `Q-03` Coverage gate?

_ControlSight demands 100% branch coverage on business logic. Confirm for this project._

**Answer:** 100% on business logic, 80% overall, enforced

### ✅ `Q-04` For the automated UI walkthroughs, which surface should I drive?

_Driving the web build with a real browser is fastest and I can do it unattended; device testing needs an emulator running._

**Answer:** Web build in a headless browser — fast, unattended, run constantly

## 16 · DevOps

### ✅ `OP-01` CI — do you want GitHub Actions configured?

_Needs a GitHub remote. The repo isn't currently a git repo with a remote._

**Answer:** No CI, local checks only

### ✅ `OP-02` Should I commit as I go?

_I default to not committing unless asked. For a run this long, frequent commits give you a way to review and revert._

**Answer:** Yes — small, conventional commits per feature, on a branch

### ✅ `OP-03` Keep ControlSight's CHANGELOG + MAJOR.FEATURE.PATCH versioning discipline?

_It's a strong practice but adds a step to every change._

**Answer:** Yes — every change gets a CHANGELOG entry and version bump

### ✅ `OP-04` Any secrets/keys you can give me beyond the OpenRouter key? (Mapbox, ElevenLabs, api.bible, OpenAI…)

_Determines which features I can actually test end-to-end versus mock._

**Answer:** None

## 17 · Working Agreement

### ✅ `W-01` How should I handle a question you haven't answered yet?

_This is the core of the async loop you asked for. It sets the fleet's default behaviour when blocked._

**Answer:** Pick the recommended option, note the assumption, keep moving — flag it for later review

**Note:** PLUS: the Question Hub is now a first-class second project. Requirements: MCQs everywhere with an "Other" free-text escape; reference images (mockups, swatches) attached to questions; rich artifacts (diagrams, comparisons, code); a continuous listener that wakes the orchestrator the moment an answer lands; make it genuinely enjoyable to answer; and test the platform as rigorously as the app itself.

### ✅ `W-02` How many agents should run at once?

_More parallelism is faster but harder to keep coherent, and every agent burns tokens._

**Answer:** As many as the work allows, you manage it

### ✅ `W-03` How often do you want a progress report?

_So I can pace summaries to your attention rather than spamming you._

**Answer:** Every time I check the hub — a short status at the top

### ✅ `W-04` Should I keep the Question Hub running for the whole project as the async channel?

_It's how you'll answer everything the fleet surfaces from here on._

**Answer:** Yes — keep it up, keep posting questions there

### ✅ `W-05` Is there anything you'd consider out of bounds? (files not to touch, things not to install, costs not to incur)

_You gave me broad freedom; I'd like the edges of it stated explicitly._

**Answer:** Default 

**Note:** The default is fine 

### ✅ `W-06` Anything else I should know that I haven't asked about?

_Open floor — deadlines, audiences, demos coming up, personal preferences, pet peeves._

**Answer:** Nothing i can think of

## 18 · From the fleet

### ✅ `Q-005` The Flutter app has TWO separate AI conversation surfaces: a global 'Ask' page with saved thread history, and a chapter-scoped 'Chat' panel inside the reader that auto-grounds every question in the current passage and persists per chapter. Keep both in the rewrite, or unify?

_They use the same /chat/stream endpoint but different persistence (ask_threads vs reading_chats), different UI, and different grounding. Building both doubles the chat surface area; the in-reader grounded chat is the higher-value one and matches pillar 2 (point-of-need intelligence)._

**Answer:** One unified chat that carries passage context when opened from the reader

### ✅ `Q-006` The Flutter reader is desktop/tablet-first: a resizable context rail, a two-pane Ask split, an icon rail and a 232dp sidebar. Does the Expo app need tablet/desktop layouts at all, or is it phone-only?

_Roughly a third of the Flutter UI code exists purely to serve the >=600dp and >=1100dp breakpoints (resizable_split.dart, the rail shell, the artifact canvas). Porting them costs real time and RN has no built-in split pane. If phone-only, that code is dropped entirely._

**Answer:** Full phone/tablet/desktop parity with the Flutter app

### ✅ `Q-007` Atlas Bible ships pre-computed enrichment records. The best culture/history and original-language sources (unfoldingWord UHB, UGNT, en_tn; Tyndale OSN; Theographic Places) are CC BY-SA 4.0 (share-alike). Do we accept share-alike on the enrichment DB, or keep CC BY-SA data in separate tagged tables and never blend it into the pre-computed passage JSON?

_This decides whether the passage_id enrichment record can mix sources freely. If share-alike is unacceptable, Word Root and Cultural badges must be rebuilt from CC BY / PD sources only (STEPBible TAHOT/TAGNT + OpenScriptures HebrewLexicon + Dodson + HelloAO PD commentaries), which is a different and larger pipeline. The old repo locked this as 'keep table-scoped' (bible-enrichment/DECISIONS.md #2) but that predates the pre-computed-record design._

**Answer:** Server-side only: never redistribute the DB, so share-alike never triggers

### ✅ `Q-008` CORRECTED: only TWO badges are now genuinely dataset-less — 3D City and Meditate (plus OT year_approx). Chiasm and Manuscript both turned out to have CC BY 4.0 sources. How do we handle the remaining two?

_Verified live: STEPBible TAGNT (CC BY 4.0) gives canon-wide NT manuscript variants; Murai Literary Structure (CC BY 4.0) gives 10,304 chiastic nodes. For 3D City the closest candidate is CC BY-NC-ND (fails on both counts) and for Meditate nothing openly licensed exists at all. So 8 of 10 badges are sourceable; these two are not._

**Answer:** LLM-generate Meditate prompts (human-reviewed); ship 3D City as a stylised 2.5D site sheet

### ✅ `Q-009` The spec's passage_id (ACTS_16_11_15) is a passage-range key, but every open enrichment dataset joins on a single verse (OSIS John.3.16 / integer 43003016). Who defines passage boundaries, and do we store enrichment per-passage, per-verse, or both?

_Cross-references, place mentions, verse words and translation notes are all verse-keyed. A pre-computed per-passage record needs a pericope table that does not exist in any verified open dataset. Getting this wrong means either re-cutting every enrichment record later, or shipping badges that cannot light up on individual verses._

**Answer:** Both, denormalised: verse rows for badges, passage rows for the map/timeline canvas

### ✅ `Q-010` OpenRouter sells no embedding models at all (verified: 0 of 388). Where should pgvector embeddings come from?

_Grounded RAG needs an embedding model and OpenRouter cannot supply one, so this needs a second source. Self-hosting BGE-M3 or nomic-embed-text in the existing Docker stack costs $0 per embedding forever and keeps the whole $2 for generation; a paid API (OpenAI/Voyage/Cohere) is less setup but adds a second vendor, a second key, and a per-embedding cost the spend guard cannot see._

**Answer:** Pay a separate embedding API (OpenAI text-embedding-3-small ~$0.02/M tokens) and accept a second vendor + key

### ✅ `Q-012` Generated enrichment is cheap (~$0.40 for the whole NT) but human theological review is not. What is the shipping bar for LLM-generated badge content in v1?

_docs/architecture/ai-model-strategy.md section 5 shows budget is not the constraint - review effort is. Q-008's default is to LLM-generate the four dataset-less badges flagged as origin='generated'. Nobody has said who reviews that content or how much of it must be reviewed before a reader sees it. This sets the size of the content pipeline's human stage and therefore how much of Acts can actually ship._

**Answer:** Every generated record is human-reviewed before it is visible - Acts only (28 chapters, ~230 records)

> **Amended `2026-08-29` — see `Q-024`.** The "Acts only" cap above was cost-driven, on the
> assumption enrichment content would be generated by a paid third-party API. It is not: the
> coding agent authors this content directly, at zero incremental API cost, the same way it
> writes code. The gate was never money — see `Q-024` for the corrected framing.

### ✅ `Q-013` What should the app's permanent identifiers be — iOS bundle id, Android package, and deep-link URL scheme?

_The scaffold has to write them into app.json now, and they are effectively permanent: the bundle id and package name cannot be changed after the first store submission without shipping a new app, and the scheme is what every share link and badge deep link resolves against. I have provisionally used app.atlasbible.mobile / atlasbible. If you own a domain or an existing developer account with a different reverse-DNS prefix, changing it later is cheap only until the first build is submitted._

**Answer:** app.atlasbible.mobile + scheme atlasbible (what the scaffold used)

### ✅ `Q-014` TAGNT's manuscript-variant layer changes the Manuscript badge from 'no dataset' to 'canon-wide NT coverage'. Do we ship it as sourced data instead of ~25 hand-curated variants?

_ROADMAP §4 marks Manuscript as no-dataset and routes it to hand-curation for M10. Verified this run: STEPBible TAGNT (CC BY 4.0, acquired, 30MB) tags every NT word with the manuscript families attesting it (N=Nestle-Aland / K=Textus Receptus / O=other) plus the specific editions. Its own legend counts ~3,202 translation-altering variant words across the NT. Spot-checked correct on Acts 8:37 (TR-only) and the Comma Johanneum in 1 John 5:7. Caveat: this is edition-level attestation, not per-manuscript sigla (no P46, Aleph, B individually) so witness cards cannot be built from it alone._

**Answer:** Ship TAGNT-sourced variants, and hand-curate witness sigla for ~25 famous variants on top

### ✅ `Q-015` The only open chiasm/pericope dataset (Hajime Murai, CC BY 4.0) is one scholar's analysis. Do we ship interpretive structural analysis attributed to a named scholar?

_ROADMAP §4 marks Chiasm/Structure as no-dataset, L effort, generate-and-review. Verified this run: bible.literarystructure.info by Hajime Murai publishes CC BY 4.0 spreadsheets covering the whole canon — 1,959 pericopes, 10,304 labelled chiastic nodes (Acts alone: 49 pericopes, 344 nodes), in exactly the A/B/C/D/C'/B'/A' shape the spec's key_chiastic_nodes[] wants. Acquired. It also answers Q-009 (no open pericope dataset) for free. Two caveats: chiastic structure is interpretive, not consensus fact; and the English column contains NAB/NRSV/NJB verse text that is NOT Murai's to license and must be dropped at ingest._

**Answer:** Ship it, attributed inline as Murai's reading

### ✅ `Q-016` The only open per-passage dating (Theographic Events.csv, MetaV) is Ussher's biblical-literalist chronology. Do we ship it, and if so for which books?

_ROADMAP §4 marks History year_approx as no-dataset, L effort. Verified this run: Theographic Events.csv has 450 events, all dated, 137 in the AD era, each linked to an explicit OSIS verse list — for Acts every pericope-scale event has a year. But its first row is 'Creation of all things', startDate -4003, i.e. Ussher's 4004 BC; and the independent MetaV dataset states the same lineage verbatim in its readme ('Source: Annals of the World, James Ussher'). For NT-era passages Ussher is broadly uncontroversial and usable. For OT passages it encodes a theological position that mainstream scholarship rejects, and surfacing it as neutral fact would be a credibility problem. Theographic is also CC BY-SA._

**Answer:** NT-era only (Acts MVP is unaffected); leave OT year_approx unsourced

### ✅ `Q-017` ink.tertiary (#5D6A7D) fails WCAG AA as small text — 3.36:1 on bg.card against the 4.5:1 minimum. The design language assigns it to labels and metadata, which are 9-11pt. How should the design system resolve that?

_D-06's default sets the bar at 'AA for text contrast', but design-language.md §2 assigns the dimmest ink to the smallest type. One of the two has to give. Measured: 3.67:1 on bg.canvas, 3.47 on bg.elevated, 3.36 on bg.card, 3.15 on bg.cardHover. Every other text pair in the palette passes comfortably (gold 10.81:1, cyan 11.07:1, ink.secondary 7.61:1), so this is the single failing pair._

**Answer:** Use ink.secondary (#93A0B4, 6.53:1 worst case) for all 9-11pt metadata; keep ink.tertiary for large text, icons and rules only

### ✅ `Q-018` The PRD says '10 Embedded Feature Badges' but lists 11 marks, and the design language assigns a hue to only 10 (Lineage has none). Which is correct?

_The badge union is a closed discriminated type. Dropping Lineage loses a specified sheet (3D family tree); keeping it means the design system owes Lineage a hue before the reader can learn badges by colour._

**Answer:** Eleven badges: keep Lineage, design assigns it a hue

### ✅ `Q-019` In a pre-computed passage_id like ACTS_16_11_15, is the book segment the uppercased OSIS code (1COR) or the uppercased full name (1CORINTHIANS)?

_passage_id is the cache key, CDN path segment, and analytics dimension for every pre-computed record. The PRD's only example is ACTS, where the two are identical, so the spec cannot settle it. Choosing differently from the editorial pipeline means every lookup misses and the pipeline re-runs._

**Answer:** Uppercased OSIS code — ACTS_16_11_15, 1COR_13_1_13

### ✅ `Q-020` The AI spend guard now enforces a hard, non-overridable ceiling. What should its absolute maximum be — the number no environment variable can exceed?

_packages/ai-guard clamps ATLAS_AI_CEILING_USD at a hard-coded ABSOLUTE_MAX_CEILING_USD, currently $2.00, with defaults of $0.50 locally and $0.05 under CI. CLAUDE.md and the brief both say the key holds ~$2, but docs/architecture/ai-model-strategy.md measured $4.57 actually remaining. Set too low, a legitimate pre-compute run (whole New Testament is ~$0.60) gets blocked; set too high, the guard stops being a guard. Raising the constant requires a code change on purpose, so this is worth getting right once._

**Answer:** Keep $2.00 absolute max, $0.50 local / $0.05 CI defaults — treats the ~$2 figure as the real budget

### ✅ `Q-021` Which icon family do the ten inline badge glyphs use?

_design-language.md 5 says the badge's text AND icon are rendered in the badge's full hue. Colour emoji cannot be tinted - the OS draws them in its own palette - so emoji glyphs break the one rule that makes the ten badge types learnable by colour. They also carry inconsistent metrics across Android/iOS/web, which shifts the pill width per platform. The inline badge spike currently ships emoji as a stopgap._

**Answer:** A monochrome vector icon set drawn as react-native-svg paths, vendored in-repo (10 icons, tintable, identical on every platform)

### ✅ `Q-022` When a grounded-chat stream drops mid-answer (socket dies, app backgrounded, tunnel flaps), should the client silently retry the turn from the start, or keep the partial text and show a Retry control?

_A silent retry re-runs the whole model call, so every dropped stream costs a second answer against the $4.57 OpenRouter budget, and the user can see the reply restart from the beginning. A Retry control costs nothing until the user asks, but leaves a visibly truncated answer on screen. There is no resume: the /chat/stream endpoint is stateless and cannot continue a partial completion. This decides the failure UX for the Ask tab and the reader chat, and it is cheaper to settle before the bubble is built._

**Answer:** Keep the partial text and show a Retry control — never spend without a tap

### ✅ `Q-023` Does the 300-line file limit apply to Markdown documents, or only to source files?

_Five docs written this session are over it (hub-platform.md 932, dataset-validation.md 605, question-hub/README.md 515, DEVELOPMENT.md 451, spike-sse.md 372), and the briefing itself blesses the pre-existing 760-line flutter-port-map.md. CLAUDE.md:126 states the limit unscoped, but rule 5.4.2 sits under 'File Organization Rules'. Either the docs get split into many small files (more navigation, more cross-links to keep in sync) or the rule is explicitly scoped to code. No source file exceeds 300 lines today, so this only affects prose._

**Answer:** Scope the limit to source files; documents are judged by usefulness, not length

### ✅ `Q-024` `Q-012` capped generated enrichment at Acts (28 chapters) against a $0.10-$0.40 AI budget. Is that cap still the right one now that the coding agent authors this content directly rather than calling a paid model API at pipeline time?

_`ai-model-strategy.md` §5 already concluded pre-computing the whole New Testament costs about
$0.60 through a third-party API — "scope is not budget-limited; it is time- and quality-limited."
That conclusion applies even more directly once a human author (the coding agent, reviewed by the
product owner) writes the four dataset-less badges (Structure/Chiasm, Cultural, Meditate, History
dating rationale) directly, the same way it writes source code: there is no per-token bill at all
for this path. Two cost surfaces stay genuinely distinct and must not be conflated:_

_1. **Badge enrichment content** (Structure, Cultural, Meditate, History rationale) — authored
   once per passage, stored like any other row, served free forever after. No live inference, no
   per-reader cost, whether authored by a human, by the coding agent, or (later, at higher volume)
   by an automated pipeline against `ai-model-strategy.md`'s own budget math._
_2. **Studio Assistant / grounded chat on a reader's own question** — genuinely un-authorable in
   advance; every turn is a live, metered API call (`$0.000385`/turn per §5) that scales with
   usage. This is the one surface with an ongoing real cost, and the correct one to meter, cap
   with the spend guard (`Q-020`), and eventually monetize/bill per the product's pricing model._

**Answer:** Drop the Acts-only cost cap for authored badge content — the coding agent authors it
canon-wide over time, gated only by review bandwidth (per book, sequenced Acts-first since it is
the spec'd MVP target). The `$0.10`/`$0.40` figures in `ai-model-strategy.md` remain the correct
reference **only** for (a) a future automated third-party generation pipeline, should one ever
replace hand-authoring at scale, and (b) the Studio Assistant's live chat, which is the sole
feature that must be metered and billed per use.

